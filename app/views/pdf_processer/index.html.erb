<fieldset>

	<legend>Upload a set of payslip to split them</legend>
	<br>

	<%= form_tag({:action => :send_pdf}, :multipart => true) do %>
		<label for="file">File to Upload </label><%= file_field_tag 'pdf' %>
		<%= submit_tag "Upload payslips" %>
	<% end %>

	<p><strong>Note:</strong> Input should be a pdf batch file (pay slip) consisting of multiple pages - each page will pertain to a single employee. The application will parse the name of the employee, split the pdf batch file into individual pages, and then place that sheet into the specific employee's folder.</p>

</fieldset>